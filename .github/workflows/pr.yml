name: "Pull Request Labeler"
on:
  pull_request_target  # ← Dangerous: runs with base repo permissions

jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v5  # ← No checkout step!
